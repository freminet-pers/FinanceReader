# v2.23.6 Release maintenance note

This is a repository-maintenance aid, not a new release and not a replacement for the existing APK. It records what was visible on the public Release page on 2026-09-22 and gives maintainers a copy-ready body for a future edit.

## Current public state

- Published tag and title: `v2.23.6` / `财经速读 v2.23.6`.
- Published asset: `FinanceReader-v2.23.6-release.apk` (`119,457,117` bytes).
- GitHub asset digest: `sha256:6fbad16701cf77b534e11970c27e7237be407c39dd05dba889df219795518524`.
- The APK is described as arm64 (`arm64-v8a`) and the app version is `2.23.6` / versionCode `4058`; minimum Android is 10 (API 29).
- The release body records build and test checks, but does not provide a detached signature or public signing-certificate fingerprint. This note does not add a cryptographic claim that cannot be verified here.
- The Releases list also contains a draft with the same `v2.23.6` tag/title and no uploaded assets. Do not delete or publish that draft as part of this PR; a maintainer should decide whether to keep, update, or discard the duplicate after checking the release history.

## Copy-ready body for the published release

The following can be pasted into the existing published release after a maintainer rechecks the claims. It deliberately keeps the existing changes and checksum, then adds installation limits and user-facing caveats.

```markdown
# 财经速读 v2.23.6

## 安装

- 下载下方的 `FinanceReader-v2.23.6-release.apk`，适用于 arm64-v8a / Android 10（API 29）及以上。
- 这是侧载 APK；来自其他来源的 APK 可能使用不同签名，升级前请确认来源一致。
- 应用不内置 API Key。翻译费用、限额、数据保留和可用性由你配置的服务商决定。

## 更新内容

- 补齐 DeepSeek V4.1、Qwen-MT 和“启动后自动翻译全部标题”相关设置文案的多语言资源，覆盖项目现有 48 个语言包。
- 修复自动翻译开关、显示已翻译标题开关或 API 配置变化时，旧的后台翻译请求覆盖当前状态的问题。
- 保留列表标题翻译缓存，关闭自动翻译后仍可按设置显示已翻译标题。
- 详情页继续复用列表标题译文，并在打开文章时自动翻译 RSS 简介；全文翻译仍由顶部操作控制。

## 验证记录

- `48` 个 `strings.xml` 资源文件解析通过。
- `ktlintCheck`、`testFdroidDebugUnitTest` 和 `assembleFdroidRelease` 通过。
- 包名：`com.financereader.app`；版本：`4058 / 2.23.6`；目标 ABI：`arm64-v8a`。
- APK：`FinanceReader-v2.23.6-release.apk`，`119,457,117` bytes。
- SHA-256：`6fbad16701cf77b534e11970c27e7237be407c39dd05db889df219795518524`。

## 已知限制

- RSS 地址、全文抓取和文章版权由第三方出版方决定，源可能限流、改 URL、删改内容或暂时不可用。
- 远程翻译会把用户选择的文章文本和请求参数发送到配置的服务商；请先查看其隐私条款，并留意自动翻译产生的请求费用。
- 本项目是独立的 Feeder fork，不是 Feeder 官方发行版；问题请在 FinanceReader 仓库反馈。
```

## Maintainer checklist

- [ ] 先在 GitHub Release 编辑页确认已发布的 `v2.23.6` 是要更新的对象，不要替换或重新上传现有 APK。
- [ ] 决定同 tag 草稿的去留，并避免让 Release 列表继续显示两个同名 `v2.23.6`。
- [ ] 如果要声称“正式签名”，同时提供可公开核验的证书指纹或签名文件；否则仅保留 APK 和 GitHub asset digest 的事实描述。
- [ ] 发布正文与根目录 README 的版本、文件名、ABI、最低 Android 版本和校验值保持一致。
